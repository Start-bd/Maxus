{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "python -m uvicorn app.main:app --reload",
  "framework": "others",
  "env": {
    "ENVIRONMENT": "production",
    "DATABASE_URL": "sqlite:////tmp/manus_agent.db"
  }
}
